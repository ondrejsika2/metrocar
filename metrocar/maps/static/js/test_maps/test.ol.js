// Generated by CoffeeScript 1.3.1
(function() {

  define(['jquery', 'tests/chai', 'maps/ol'], function($, _arg, ol) {
    var assert, should;
    assert = _arg.assert, should = _arg.should;
    should();
    return describe('maps', function() {
      return describe('ol (OpenLayers)', function() {
        var f, olMap, _i, _len, _ref, _results;
        olMap = ol.createMap($('<div/>')[0]);
        _ref = ['onMoved', 'getBounds', 'setBounds', 'drawRoute', 'drawMarker', 'drawIconMarker', 'drawMarkers', 'clear', 'focus'];
        _results = [];
        for (_i = 0, _len = _ref.length; _i < _len; _i++) {
          f = _ref[_i];
          _results.push(it("should support " + f, function() {
            return (typeof olMap[f]).should.equal('function');
          }));
        }
        return _results;
      });
    });
  });

}).call(this);
